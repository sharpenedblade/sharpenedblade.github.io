---
title: Home
layout: layout.liquid
---
<h1 id ="name"> sharpenedblade.github.io </h1>
{% for post in collections.posts %}
    <article>
        <a href="{{ post.url }}" >{{ post.data.title }}</a>
        <p>{{ post.date | date: "%Y-%m-%d" }} - {{ post.data.description }}</p>
    </article>
{% endfor %}
